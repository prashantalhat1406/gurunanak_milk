import React from "react";
import "../styles/header-style.css";

const Header = ({ name }) => {
  return (
    <header className="app-header-curved">
      
      {/* <h1>{name}</h1> */}
      {/* <img src={("../assets/logo.png")} alt="Gurunanak" className="logo-img" /> */}
    </header>
  );
};

export default Header;
